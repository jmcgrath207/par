apiVersion: v1
kind: Service
metadata:
  name: par-manager
  labels:
    par.dev: manager
spec:
  selector:
    par.dev: manager
  ports:
    - name: dns
      protocol: UDP
      port: 53
      targetPort: dns
